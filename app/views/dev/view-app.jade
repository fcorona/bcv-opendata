extends ../layout

block content
  div.page-header
    h1= app.name 
      small.pull-right
        a(href='/dev/apps/') volver


  dl.dl-horizontal
    
    dt Logo
    dd.thumbnail
      img(src=app.logoUrl||'')

    dt P&aacute;gina
    dd= app.url

    dt Descripci&oacute;n corta
    dd= app.shortDescription

    dt Descripci&oacute;n
    dd= app.description

    dt Llave de Acceso
    dd#key
      code= app.key ? app.key.key : '<no hay llave>'
      p Esta llave es &uacute;nica y de uso privado para esta aplicaci&oacute;n,
        | su uso esta limitado a 
        b= app.key ? app.key.limit : 0
        | &nbsp;solicitudes por hora.
    dd 
      a.generator(href='/dev/apps/#{app.id}/generateKey') recrear mi llave

  hr
  div.pull-right
    a.btn.btn-default(href='/dev/apps/#{app.id}/edit') Editar
